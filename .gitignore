# Harness run artifacts (local per-machine history + generated dashboard)
harness/history.jsonl
harness/dashboard.html

# Filesystem junk
.DS_Store
.fuse_hidden*

# Local app secrets (mirror of backend/.env — copy AIBackendSecrets.swift.example)
LockIn Set Tracker/AIBackendSecrets.swift
